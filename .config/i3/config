# FONTS
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.

# i3 config file (v4)
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Initializing Variables
set $ctrl Control
set $mod Mod1
# Mod4 is super key

# Suckless 
set $term st

#Workspace settings 
set $ws1 "1:  "
set $ws2 "2:  "
set $ws3 "3:  "
set $ws4 "4:  "
set $ws5 "5:  "
set $ws6 "6:  "
set $ws7 "7"
set $ws8 "8:  "
set $ws9 "9:  "
set $ws10 "10:  "

###---Audio and Music Definitions---###
###For mpc:
set $pause --no-startup-id mpc toggle
set $trupause --no-startup-id mpc pause
set $next --no-startup-id mpc next
set $prev --no-startup-id mpc prev
set $lilfor --no-startup-id mpc seek +10
set $bigfor --no-startup-id mpc seek +50
set $lilbak --no-startup-id mpc seek -10
set $bigbak --no-startup-id mpc seek -50
set $beg --no-startup-id mpc seek 0%

# FOR ALSA/AMIXER
set $inc --no-startup-id amixer -c 1 sset Headphone 2%+
set $biginc --no-startup-id amixer sset Master 5%+
set $dec --no-startup-id amixer -c 1 sset Headphone 2%-
set $bigdec --no-startup-id amixer sset Master 5%-
set $mute --no-startup-id amixer sset Master toggle
set $truemute --no-startup-id amixer sset Master mute
set $headphonetoggle --no-startup-id amixer -c 1 sset Headphone toggle

# Color variables 
set $bg-color            #282c34FF
set $bf-color            #282c34FF
set $bd-color            #282c34FF
set $inactive-bg-color   #282c34FF
set $inactive-bd-color   #282c34FF
set $text-color          #00000000
set $inactive-text-color #00000000

# set $flash --no-startup-id bash ~/.Scripts/flash.sh

# Key-Bindings
# Nothing to change
# Switch to workspace
    bindsym $mod+1      workspace $ws1
    bindsym $mod+2      workspace $ws2
    bindsym $mod+3      workspace $ws3
    bindsym $mod+4      workspace $ws4
    bindsym $mod+5      workspace $ws5
    bindsym $mod+6      workspace $ws6
    bindsym $mod+7      workspace $ws7
    bindsym $mod+8      workspace $ws8
    bindsym $mod+9      workspace $ws9
    bindsym $mod+0      workspace $ws10
# move focused container to workspace
    bindsym $mod+Shift+1    move container to workspace $ws1
    bindsym $mod+Shift+2    move container to workspace $ws2
    bindsym $mod+Shift+3    move container to workspace $ws3
    bindsym $mod+Shift+4    move container to workspace $ws4
    bindsym $mod+Shift+5    move container to workspace $ws5
    bindsym $mod+Shift+6    move container to workspace $ws6
    bindsym $mod+Shift+7    move container to workspace $ws7
    bindsym $mod+Shift+8    move container to workspace $ws8
    bindsym $mod+Shift+9    move container to workspace $ws9
    bindsym $mod+Shift+0    move container to workspace $ws10

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    bindsym $mod+Shift+r        restart 
# exit i3 (logs you out of your X session)
    bindsym $mod+Shift+e        exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Workspace back and forth
    bindsym $mod+Tab               workspace back_and_forth
    bindsym $mod+n                 workspace next
    bindsym $mod+semicolon         workspace prev

# Spawning Terminal    
    bindsym $mod+apostrophe     split horizontal ;; exec $term
    bindsym $mod+slash          split vertical ;; exec $term

# kill focused window
    bindsym $mod+q              kill

    bindsym $mod+h              focus left
    bindsym $mod+j              focus down
    bindsym $mod+k              focus up
    bindsym $mod+l              focus right

    bindsym $mod+Shift+h        move left
    bindsym $mod+Shift+j        move down
    bindsym $mod+Shift+k        move up
    bindsym $mod+Shift+l        move right

    bindsym $mod+space           focus mode_toggle
    bindsym $mod+Shift+space     floating toggle

# Scratchpad 
    bindsym $mod+w                    move scratchpad 
    bindsym $mod+u                    scratchpad show; resize set 600 400

# Sticky toggle
    bindsym $mod+i                    sticky toggle

# Dynamic resize
    bindsym $mod+Shift+y	resize shrink width 5 px or 5 ppt
    bindsym $mod+Shift+i	resize shrink height 5 px or 5 ppt
    bindsym $mod+Shift+u	resize grow height 5 px or 5 ppt
    bindsym $mod+Shift+o	resize grow width 5 px or 5 ppt
# Gaps
    bindsym $mod+Shift+d 	gaps inner current set 12; gaps outer current set 13
    bindsym $mod+t              gaps inner current set 6; gaps outer current set 6
    bindsym $mod+z              gaps outer current plus 2
    bindsym $mod+Shift+z        gaps outer current minus 2
    bindsym $mod+f              fullscreen toggle

# Bindings to execute    
    bindsym $mod+x                      exec --no-startup-id ~/.config/i3/lock.sh
    bindsym $mod+Shift+p                exec psensor
    bindsym $mod+Shift+m                exec transset -a --dec .05
    bindsym $mod+Shift+n                exec transset -a --inc .05
    bindsym $mod+Shift+s                exec dmenu_run
    bindsym $mod+Shift+g                exec qutebrowser 
    bindsym $mod+Return                 exec $term 
    bindsym $mod+Home                   exec thunar
    # bindsym $mod+Home                   exec nautilus --no-desktop

# Music Bindings
# For advancing forward/backward in an mpd song
    bindsym $mod+bracketleft                exec $lilbak
    bindsym $mod+Shift+bracketleft          exec $bigbak
    bindsym $mod+bracketright               exec $lilfor
    bindsym $mod+Shift+bracketright         exec $bigfor

    bindsym $mod+greater                exec $next
    bindsym $mod+less                   exec $prev
    bindsym $mod+Shift+less             exec $beg
    bindsym $mod+p                      exec $pause && mpc volume 40

    bindsym $mod+Up                     exec $inc
    bindsym $mod+Down                   exec $dec
    bindsym XF86AudioRaiseVolume        exec $inc
    bindsym XF86AudioLowerVolume        exec $dec
    bindsym XF86AudioMute               exec $headphonetoggle

# Unused bindings
# bindsym $mod+Shift+slash 
# bindsym $mod+Shift+greater 
# bindsym $mod+Shift+x          
# bindsym $mod+s          
## bindsym $mod+g                      
# bindsym $mod+Shift+Home 
# bindsym $mod+Shift+apostrophe
# bindsym $mod+o
# bindsym $mod+u
# bindsym $mod+y
# bindsym $mod+Shift+semicolon 
# bindsym $mod+Shift+w 
# bindsym $mod+e
# bindsym $mod+r
# bindsym $mod+Shift+c reload
# bindsym $mod+c
# bindsym $mod+Shift+a 
# bindsym $mod+i 
# bindsym $mod+Shift+t 
# bindsym $mod+b 
# bindsym $mod+Shift+b 
# bindsym $mod+Shift+f 
# bindsym $mod+Shift+v 
# bindsym $mod+v 
# bindsym $mod+n
# bindsym $mod+m 
# bindsym $mod+Shift+Right 
# bindsym $mod+Shift+Left 
# bindsym $mod+Shift+Down
# bindsym $mod+Shift+Up 
# bindsym $mod+Left 
# bindsym $mod+Right
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
# i3-dmenu-desktop (Only shows .desktop files)
# bindsym $mod+Shift+q
# bindsym $mod+Shift+Return

# EXEC AND EXEC_ALWAYS
exec qutebrowser 
exec --no-startup-id mpd
exec --no-startup-id transmission-daemon -g /home/instincts/.config/transmission-daemon
exec --no-startup-id nm-applet

# Unclutter makes the mouse invisible after a brief period
exec --no-startup-id unclutter

exec --no-startup-id compton --config ~/.config/compton/config -b 
exec feh --bg-fill /home/instincts/Downloads/Landscapes/Dark/1491186979062.jpg 
exec_always bash xrdb ~/.Xresources
exec_always --no-startup-id bash $HOME/.config/polybar/launch.sh
exec --no-startup-id xmodmap ~/.Xmodmap
exec_always --no-startup-id ~/Solstice/bash_scripts/alsa/alsa.sh
exec_always --no-startup-id xset r rate 250 60 
focus_follows_mouse no


popup_during_fullscreen smart

# Mount other drives:
exec --no-startup-id sudo mount -a

# SETTINGS
# bar {
#     status_command i3blocks -c ~/i3blocks.conf
#         position top
#         font xft:Source Code Pro Medium 9 
#         mode dock
#         modifier None
# } 



# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#For no window bar
for_window [class="^.*"] border pixel 3
hide_edge_borders both

#inner and outer gaps
gaps inner 12
gaps outer 14

# client.focused              $bf-color           $bf-color       $text-color          $bf-color  $bf-color  
#859900cc
# client.focused                  #ff7f7fFF #ff7f7fFF #fdf6e3FF #ff7f7fFF

# #                           border              background      text              indicator  child border 
client.focused                  #c5a9f9FF #c5a9f9FF #fdf6e3FF #c5a9f9FF
client.unfocused            $inactive-bd-color  $bg-color       $text-color          $bg-color  $inactive-bd-color
client.focused_inactive     $bg-color           $bg-color       $inactive-text-color $bg-color  $bg-color
client.urgent               $bf-color           $bg-color       $text-color          $bg-color  $bf-color
client.background                               $bg-color

# Spawning programs on different workspaces
for_window [class="qutebrowser"] move to workspace $ws1
for_window [class="libreoffice"] move to workspace $ws4
for_window [class="google-chrome-stable"] move to workspace $ws3
for_window [class="google-chrome"] move to workspace $ws3

# Remaps the caps lock button to escape and uses the US international keyboard with dead keys
exec_always --no-startup-id setxkbmap -layout us -variant altgr-intl -option caps:escape

exec_always --no-startup-id killall -q keynav && keynav &



